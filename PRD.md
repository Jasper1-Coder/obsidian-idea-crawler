# 产品需求文档（PRD）：内容素材采集 → 作品集沉淀 自动化工具

> 本文档以**雇主品牌运营 / 内容营销 / GTM 方向求职者**为使用主线撰写，尤其适合：
> 正在实习做雇主品牌运营、同时准备秋招作品集、目标岗位为 **品牌运营 / Marketing / GTM** 的同学。
> 工具本身内容无关，本文档给出的"落地场景"可替换为你自己的实习 / 项目。

---

## 1. 背景与目标

### 1.1 痛点（以目标用户视角）
- 做雇主品牌运营 + 准备 brand/marketing/GTM 作品集，需要大量优质参考：竞品打法、内容结构、GTM 拆解、面试作品集范本。
- 这些内容散落在小红书 / 抖音 / B站，看完就忘；手动截图、转写、归类极费时，且不成体系。
- 想让"采集优质内容 → 结构化 → 沉淀为作品集素材 / 内容灵感"形成一条可重复的流水线。

### 1.2 目标
- **一键采集**：给一条链接，自动产出结构化笔记（图文 / 视频转写）。
- **自动分析**：产出"核心观点 / 关键要点 / 可落地想法 / 落地分析"四段，直接服务于内容创作与作品集。
- **作品集沉淀**：把采集+分析持续归入主题库（内容结构 / 竞品打法 / GTM 框架），随时能抽取成作品集章节。
- **跨平台可装**：Windows / macOS / Linux 复制文件夹即迁移，setup 一键装环境。

---

## 2. 适用范围与前提

- **适合谁**：做内容运营 / 雇主品牌、想进 brand·marketing·GTM 的人；正在准备求职作品集的同学。
- **当前落地场景示例**：在 XBotGo 实习做雇主品牌运营（小红书 / 公众号 / 视频号），用本工具采集行业优秀内容与方法论，反哺自己的内容产出与作品集。**该场景可替换为你自己的实习 / 项目。**
- 使用者需自行准备：
  - **WorkBuddy 客户端**（Mac / Windows 版，workbuddy.cn）——本工具是 WorkBuddy 本地 Skill，不是浏览器插件。
  - **Python 3.10+**（setup 自动建隔离 venv）。
  - **笔记后端（二选一，都可）**：
    - **Obsidian**：默认指向 `~/Obsidian/EdenVault/10-AI/`（用环境变量 `EDEN_VAULT` 覆盖）。
    - **Notion（本文档示例用户所用）**：工具先把笔记写成**本地 Markdown 文件**，你再把 md 作为中转同步进自己的 Notion 数据库——可手动粘贴，或后续接 Notion API 做自动同步（属可选扩展，不在本 skill 强制范围）。两种方式输出内容完全一致。
  - 对应平台 **cookie**（小红书 / 抖音网页用 Cookie-Editor 导出；B站无需）。
  - 视频转写用的 **SiliconFlow API Key**（仅视频需要）。

---

## 3. 功能需求（FR）

| 编号 | 功能 | 本场景下的用法 |
|---|---|---|
| FR1 | 小红书图文抓取 | 采集雇主品牌优秀帖 / 品牌营销方法论 / 内容结构拆解图文；自动下载图片 + 读图 OCR 补全正文 |
| FR2 | 抖音 / B站 视频转写 | 采集品牌拆解 / GTM 复盘 / 面试作品集参考视频；下载→抽音频→转写→笔记 |
| FR3 | 四段落地分析 | 脚本只出骨架，Agent 补：①核心观点 ②关键要点 ③可落地想法(Prompt 种子) ④落地分析——**本场景侧重"可写进作品集的模块 / 可复用内容结构 / 可引用策略框架"** |
| FR4 | 跨平台一键安装 | `setup` 建 venv + 装依赖 + 下载对应平台 ffmpeg（Mac 按芯片架构选 arm64/x86_64） |
| FR5 | 凭证隔离 | cookie / Key 只存本地隔离目录，不进版本库、不外传 |
| FR6 | 可移植 | 路径相对化 / 环境变量；复制文件夹即迁移 |

---

## 4. 非功能需求（NFR）

- **跨平台**：Windows / macOS（Apple Silicon & Intel）/ Linux 验证可用。
- **笔记后端无关**：产出标准 Markdown，Obsidian / Notion / 任意支持 md 的工具皆可用。
- **隐私**：凭证等同账号密码，绝不外传 / 不上传公开仓库。
- **可扩展**：新增平台只需加一个 `scripts/<platform>_pipe.py`。
- **轻量**：分享包剔除 venv / ffmpeg / cookie，仅含源码 + 文档（~22KB）。

---

## 5. 环境搭建步骤（跨平台）

1. 装 WorkBuddy 客户端（Mac / Win 版）。
2. 把 `obsidian-idea-crawler/` 放到 `~/.workbuddy/skills/`（Mac/Linux）或 `%USERPROFILE%\.workbuddy\skills\`（Win）。
3. 终端跑安装：
   - macOS / Linux：`bash setup.sh`
   - Windows：`setup.bat`
4. Cookie-Editor 从网页导出 Netscape cookie → `cookies/xiaohongshu.txt`（抖音同理）。
5. SiliconFlow Key 写进 `.venv/.env`（`SILICONFLOW_API_KEY=sk-xxx`，仅视频需要）。
6. 在 WorkBuddy 对话里把链接发给我即可；产出为本地 md，按需导入你的 Obsidian / Notion。

---

## 6. 工作流设计

### 6.1 单次触发流（已支持）
```
发链接 → 判别平台 → 抓/转 → 读图OCR / 语音转写 → 补四段分析 → 落本地 md（再按需同步 Obsidian / Notion）
```

### 6.2 作品集沉淀流（推荐自建，Notion 用户友好）
```
日常采集竞品/方法论 → 四段分析 → 按主题归入素材库（本地 md）
  ├─ 内容结构（标题钩子/图文节奏/视觉风格）
  ├─ 竞品打法（雇主品牌怎么做/品牌战役拆解）
  └─ GTM 框架（渠道组合/用户分层/转化路径）
→ 把对应 md 同步进 Notion 数据库对应表
→ 秋招时抽取成作品集章节 + 附"我的改写实践"
```

### 6.3 内容生产流（可直接反哺实习）
```
采集优秀雇主品牌帖 → 拆其结构 → 生成自己的小红书草稿（雇主品牌方向）
→ 经实习账号发布，真实数据再回流进作品集作为"实战成果"
```

### 6.4 可扩展自动化
- 定时批量处理一批待读链接，产出当日知识简报。
- 多源聚合（小红书+抖音+B站）按主题归并。
- 四段分析的"可落地想法"直接喂下游（生成草稿 / 生成方案）。

---

## 7. 适配你的领域

本工具内容无关，落地场景可替换：

| 环节 | 本场景示例 | 换成别的领域 |
|---|---|---|
| 采什么 | 雇主品牌优秀帖 / 品牌营销方法论 / GTM 拆解 | 你的实习或项目相关主题 |
| 四段"落地"侧重 | 可写进作品集的模块 / 可复用内容结构 | 按你的目标岗改写 |
| 输出落到哪 | 本地 md → 同步进 **Notion**（或 Obsidian） | 你的笔记库 / 知识库 |

**合规提醒**：作品集里引用公司内容 / 数据，注意脱敏与授权；本工具只采**公开平台内容**，不碰任何内部数据。

---

## 8. 隐私与安全红线

- cookie / API Key = 账号密码，**只存本地隔离目录，绝不外传、不上传公开仓库**。
- 分享给他人的版本必须剔除：`cookies/*.txt`、`.venv/`、`.env`、`bin/ffmpeg`。
- 使用者之间**不共享凭证**，各自导出自己的。

---

## 9. 当前状态与待办

- [x] 跨平台 skill 已打包并验证（Win / Mac / Linux）
- [x] 公开仓库 + 分享 zip 已生成（剔除凭证）
- [x] PRD 已按"雇主品牌运营 / 作品集准备"主线撰写，并适配 Notion 后端差异
- [x] 通用 Prompt 模板集（PROMPTS.md）已生成，贴合内容创作 / 雇主品牌 / GTM / 作品集场景
- [x] PRD + PROMPTS 已加入仓库，随 skill 一并发布
- [ ] 朋友具体作品集结构（待她确认后可在第 6.2 节细化）

---

*文档生成说明：以目标用户（雇主品牌运营实习 + 秋招备作品集 + 目标 brand/marketing/GTM）为主线；输出后端适配 Notion（本地 md 中转同步），工具能力通用、落地场景可替换。*
